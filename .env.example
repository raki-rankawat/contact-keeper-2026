# Copy to .env and fill in real values. Never commit .env.
PORT=5000
# The path segment after the host is the database name — omit it and mongo uses "test"
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/contact-keeper?appName=contact-keeper

# Needed once auth lands
# JWT_SECRET=
